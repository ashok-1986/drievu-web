# Page-Specific Design Overrides

Drop a `<page-slug>.md` file here when a page needs to deviate from
[`../MASTER.md`](../MASTER.md) — e.g. `dashboard.md`, `system-builder.md`.

Only document the delta from the Master (colors/typography/motion rules that
differ for this page). Don't restate the whole system — the Master is still
the baseline for anything not overridden here.

No overrides exist yet; every page currently follows the Master as-is.
